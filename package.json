{
  "name": "ffm.js",
  "version": "1.0.0",
  "main": "./built/index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "yarn tsc",
    "format": "yarn biome format --write ./src",
    "lint": "yarn biome lint --apply ./src"
  },
  "dependencies": {
    "emoji-regex": "^10.3.0",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@biomejs/biome": "1.7.3",
    "mfm-js": "^0.24.0"
  }
}
